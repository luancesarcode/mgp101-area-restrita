{
  "name": "mgp101-area-restrita",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Jogo de exploração e detecção de radiação desenvolvido com Three.js.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:browser": "playwright test",
    "lint": "eslint src tests",
    "check": "npm run lint && npm run test && npm run build"
  },
  "dependencies": {
    "three": "0.160.0"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "eslint": "10.8.1",
    "vite": "8.2.1",
    "vitest": "4.1.10"
  }
}
